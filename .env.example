# BroxStudies monorepo environment templates
#
# Setup (from repo root):
#   ./setup-env.sh
#
# Or manually:
#   cp backend/.env.example backend/.env
#   cp frontend/.env.example frontend/.env
#
# Never commit real secrets. Local .env files are gitignored.
